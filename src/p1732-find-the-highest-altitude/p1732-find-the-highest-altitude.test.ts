import { largestAltitude } from "./p1732-find-the-highest-altitude"

describe("case-01", () => {
    it("", () => {
        let gain = [-5, 1, 5, 0, -7]
        const result = largestAltitude(gain)
        expect(result).toBe(1)
    })
})

describe("case-02", () => {
    it("", () => {
        let gain = [-5, 1, 5, 0, -7]
        const result = largestAltitude(gain)
        expect(result).toBe(1)
    })
})
